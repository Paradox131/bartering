@model IReadOnlyList<bartering.Models.SwapOffer>
@{
    ViewData["Title"] = "Incoming offers";
}
<h1 class="h3 mb-4">Incoming swap offers</h1>
@if (Model.Count == 0)
{
    <div class="text-muted py-4">No incoming offers yet.</div>
}
else
{
    <partial name="_SwapOfferList" model="Model" />
}